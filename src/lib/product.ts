/** Canonical product display / package name (lowercase, hyphenated). */
export const PRODUCT_NAME = "hexa-crm" as const;

/** Browser / window document title prefix. */
export const PRODUCT_TITLE = `${PRODUCT_NAME} · Gestión de tienda` as const;

/** User-Agent for GitHub Releases update checks. */
export const PRODUCT_UPDATE_UA = "Hexa-CRM-UpdateCheck" as const;
