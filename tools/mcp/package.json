{
  "name": "@hexa-nix/nix-c-mcp",
  "version": "0.1.0",
  "description": "MCP server for Nix-C CRM — one file per agent action",
  "type": "module",
  "private": true,
  "license": "MIT",
  "bin": {
    "nix-c-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "~5.6.2"
  }
}
